{
    "config": {
        "source": {
            "type": "file"
        }
    },
    "rules": {
        "id": ["required"]
    },
    "fields": {
        "id": "uuid",
        "name": "string",
        "email": "email",
        "password": "hash",
        "gitlab_user": "string"
    }
}
