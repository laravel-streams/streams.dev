{
    "slug": "pages",
    "routes.view": "/{id}",
    "template": "page",
    "fields": {
        "title": {
            "type": "text",
            "required": true
        },
        "intro": "textarea",
        "body": "text|textarea",
        "sort": "integer"
    }
}
