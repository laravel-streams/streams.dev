{
    "handle": "pages",
    "config.source.format": "md",

    "routes": {
        "view": {
            "uri": "{entry.path}"
        }
    },
    
    "template": "page",
    "rules": {
        "id": "required",
        "title": "required"
    },
    "fields": {
        "id": "slug",
        "path": "string"
    }
}
